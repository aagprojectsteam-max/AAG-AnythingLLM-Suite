# Uninstall

`./uninstall.sh` replays the last install transaction: AAG-created targets are removed and replaced stock AnythingLLM files/systemd units are restored. Conversations, documents, unrelated configuration, models, Atlas assets and generated user content are preserved. `--purge-config` additionally removes only the AAG user config. Backups remain for manual recovery.

