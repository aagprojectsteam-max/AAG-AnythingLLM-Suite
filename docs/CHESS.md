# Chess

The chess agent uses deterministic legal-move proof as authority; Stockfish may propose or cross-check candidates but never replaces verification. Boards are rendered deterministically, hidden solutions remain private, and the bridge exposes only narrow domain operations. Stockfish itself is not bundled.

