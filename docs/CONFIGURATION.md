# Configuration

Copy `.env.example` to `.env`. All roots are configurable. Secrets must be paths to external files, not literal values in service definitions or committed configuration. Preserve ownership scoping and loopback bindings when changing ports or Docker routing.

