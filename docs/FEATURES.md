# Features

The suite consists of the seven maintained families in `COMPLETE-FEATURE-INVENTORY.md`. Forgotten functionality found during discovery includes multi-image ZIP/PDF export, safe artifact presentation, progress polling/cancellation, composer loopback proxying, offline context-window lookup, tool reranking, runtime turn/idempotency bridging, and the optional governed Ubuntu operations stack.

