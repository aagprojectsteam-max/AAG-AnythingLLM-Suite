# Local LLM

`integrations/llamacpp/aag-llama-control` preserves the accepted llama.cpp SYCL/Intel Arc and MTP architecture: model selection, mmproj discovery, approved-pair policy, `--mtp auto`, fallback, cache and lifecycle. The llama.cpp source/binary and all GGUF/mmproj/MTP files are external. The inspected llama.cpp checkout was at `a1f96d4fc` (`b10293`).

