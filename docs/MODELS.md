# Models

No weights are included. `config/models.yaml` is the authoritative dependency declaration. The installer reports missing assets and never downloads large models automatically. Users must obtain every model from its legitimate project/source, accept its license, place it under configured roots, and supply hashes where required.

Exact upstream URLs, revisions and hashes that were not established from governed local evidence remain `unresolved` deliberately.

