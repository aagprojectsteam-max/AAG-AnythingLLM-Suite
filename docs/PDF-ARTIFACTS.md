# PDF and Artifacts

The endpoint overlays validate artifact ownership, paths and MIME types, export multi-image collections, and assemble PDFs without exposing arbitrary filesystem access. Generated files and user artifacts are runtime data and are excluded.

