# Rollback

Run `./rollback.sh BACKUP_DIRECTORY`. The backup must contain `manifest.tsv` and files created by this installer. Rollback restores replaced files, removes only files recorded as newly created, and does not touch user data or models.

