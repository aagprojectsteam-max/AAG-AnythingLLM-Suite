# Troubleshooting

Start with `./doctor.sh --preflight`. Common hard failures are a mismatched AnythingLLM revision, missing model files, absent Atlas asset bundle, duplicate skill hub IDs, unavailable loopback services, or a stale compatibility token/attestation. Do not bypass ownership or attestation checks to make a test pass.

