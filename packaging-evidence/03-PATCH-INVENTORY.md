# Patch Inventory

Thirteen server mounts/additions were captured: direct image/identity commands, chat index/handler, model-map lookup, persistent and ephemeral Agent bridges, tool reranker, server endpoint registration, artifact export, PDF assembly, Composer proxy and progress/cancel. See `PATCH-MANIFEST.yaml` for target mapping and risk.

