# Model Exclusion Audit

Forbidden filename/extension scan found no GGUF, safetensors, checkpoint, VAE, mmproj/MTP, ONNX, PyTorch or inference-engine model files. `config/models.yaml` records known dependencies without inventing absent URLs or hashes.

